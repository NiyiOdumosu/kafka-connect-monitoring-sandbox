{
    "connector.class": "io.confluent.connect.jdbc.JdbcSourceConnector",
    "tasks.max": "1",
    "kafka.topic": "custom-query",
    "connection.url": "jdbc:mysql://mysql:3306/db?allowPublicKeyRetrieval=true&useSSL=false",
    "connection.user": "user",
    "connection.password": "changeme",
    "mode": "bulk",
    "topic.creation.groups": "mysql*",
    "topic.creation.default.replication.factor": 3,
    "topic.creation.default.partitions": 3,
    "query": "ALTER TABLE orders CHANGE id order_id INT; CREATE TABLE query AS (SELECT * from orders o INNER JOIN customers c ON o.customer_id = c.customer_id); SELECT* from query;"
}

